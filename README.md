# Social Media Campaign Performance Analysis

Analyze campaign spend, impressions, clicks, conversions, and revenue to identify high-performing campaigns.

## Run

```bash
python -m pip install -r requirements.txt
python analyze_campaigns.py --input data/campaign_data.csv
```

Results are saved in `outputs/` as KPI summaries, charts, and recommendations.
